import { StyleSheet } from 'react-native';

export const SvgStyle = StyleSheet.create({
  sliceCircleIcon: {
    position: 'absolute',
    top: 15,
    left: 15,
    color: '#2937f0',
  },
  unreadCountIndicator: {
    alignContent: 'center',
  },
  headerCrossIcon: {
    marginTop: 5,
  },
});
